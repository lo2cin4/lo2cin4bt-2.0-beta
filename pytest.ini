[pytest]
markers =
    smoke: fast health checks that verify the backtest pipeline still runs
    golden: deterministic snapshot tests for stable exported outputs
    regression: contract and edge-case regression tests
