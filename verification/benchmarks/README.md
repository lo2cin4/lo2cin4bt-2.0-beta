# Engine Benchmarks

This folder stores repeatable NodeIR benchmark fixtures and results.

Results are written under:

- `verification/results/benchmarks/<benchmark_id>/`

The first baseline script is:

- `verification/scripts/run_engine_benchmark.py`
