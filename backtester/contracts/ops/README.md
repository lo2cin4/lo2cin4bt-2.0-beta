# Strategy Building Blocks Contract

This folder defines the OP_SPEC contract for lo2cin4bt Strategy Building Blocks.

Phase 1 is metadata only. It records the current built-in building blocks, their
aliases, supported usage sites, temporal safety metadata, implementation paths,
and evidence paths without changing runtime backtest behavior.
