# StrategyBuilderSubAgent Reports / 策略建構報告

This folder stores repo-local strategy-building reviews.

這個資料夾保存本 repo workspace 的策略建構審查。

Use it when an AI agent checks whether a strategy idea, config, feature, or indicator follows the workspace contract.

當 AI agent 檢查策略構想、config、feature 或 indicator 是否符合 workspace contract 時，可把報告放在這裡。
